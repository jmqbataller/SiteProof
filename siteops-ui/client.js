const U='https://djhgekzhjrzszdzrzwxp.supabase.co',K='sb_publishable_xmYflMs7ohRQQvyuR516Vw_H0Ofom5P',$=x=>document.getElementById(x),e=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=v=>v?new Date(v).toLocaleString('en-PH',{dateStyle:'medium',timeStyle:'short'}):'—',days=v=>v?Math.ceil((new Date(v)-Date.now())/86400000):null;const token=new URLSearchParams(location.search).get('token');(async()=>{if(!token)return $('portal').innerHTML='<div class="panel"><div class="empty"><strong>Invalid client portal link</strong>No share token was provided.</div></div>';try{const r=await fetch(`${U}/functions/v1/siteops-client-portal?token=${encodeURIComponent(token)}`,{headers:{apikey:K}}),d=await r.json();if(!r.ok)throw Error(d.error||'Portal unavailable');const s=d.site,m=d.summary,sd=days(s.ssl_expires_at),dd=days(s.domain_expires_at);$('portal').innerHTML=`<div class="brand" style="margin-bottom:30px"><span class="brand-mark">SO</span><span>SiteOps</span><small>Client Status</small></div><div class="page-head"><div><div class="eyebrow">Website status report</div><h2>${e(s.name)}</h2><p>${e(s.url)} · Generated ${fmt(d.generated_at)}</p></div><span class="badge ${s.last_ok?'connected':'high'}">${s.last_ok?'Online':'Needs attention'}</span></div><div class="metrics"><div class="metric"><div class="metric-label">30-day uptime</div><div class="metric-value">${m.uptime_30d==null?'—':m.uptime_30d.toFixed(2)+'%'}</div></div><div class="metric"><div class="metric-label">Avg response</div><div class="metric-value">${m.avg_response_ms_30d??'—'}${m.avg_response_ms_30d?' ms':''}</div></div><div class="metric"><div class="metric-label">Open incidents</div><div class="metric-value ${m.open_incidents?'warn':''}">${m.open_incidents}</div></div><div class="metric"><div class="metric-label">Open improvements</div><div class="metric-value">${m.open_improvements}</div></div><div class="metric"><div class="metric-label">Resolved work</div><div class="metric-value good">${m.resolved_improvements}</div></div></div><div class="grid-2"><div class="panel"><div class="panel-head"><div class="panel-title">Current health</div></div><div class="panel-body"><div class="kv"><div><span>Latest status</span><b>${s.last_status_code??'—'}</b></div><div><span>Security score</span><b>${s.last_security_score??'—'}/100</b></div><div><span>SSL</span><b>${sd==null?'—':sd+' days remaining'}</b></div><div><span>Domain</span><b>${dd==null?'—':dd+' days remaining'}</b></div></div></div></div><div class="panel"><div class="panel-head"><div class="panel-title">Synthetic checks</div></div>${d.synthetics.length?d.synthetics.map(x=>`<div class="queue-item"><div class="split"><div><div class="queue-title">${e(x.name)}</div><div class="queue-meta">${fmt(x.last_run_at)} · ${x.last_response_ms??'—'} ms</div></div><span class="badge ${x.last_ok?'connected':'high'}">${x.last_ok?'Pass':'Fail'}</span></div></div>`).join(''):'<div class="empty">No synthetic checks configured.</div>'}</div></div><div class="grid-2" style="margin-top:12px"><div class="panel"><div class="panel-head"><div class="panel-title">Recent incidents</div></div>${d.incidents.length?d.incidents.slice(0,10).map(i=>`<div class="queue-item"><div class="queue-title">${e(i.title)}</div><div class="queue-meta">${fmt(i.started_at)} · ${e(i.status)}</div></div>`).join(''):'<div class="empty">No recent incidents.</div>'}</div><div class="panel"><div class="panel-head"><div class="panel-title">Recent improvements</div></div>${d.improvements.length?d.improvements.slice(0,10).map(i=>`<div class="queue-item"><div class="queue-title">${e(i.title)}</div><div class="queue-meta">${e(i.category)} · ${e(i.status)}</div></div>`).join(''):'<div class="empty">No improvement records.</div>'}</div></div>`}catch(err){$('portal').innerHTML=`<div class="panel"><div class="empty"><strong>Client portal unavailable</strong>${e(err.message)}</div></div>`}})();
